{
  "name": "scist-gate-instancer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Starts one Docker container per learner per challenge for SCIST Gate.",
  "scripts": {
    "start": "node server.mjs",
    "mock": "cross-env MOCK=1 INSTANCER_SECRET=dev-secret node server.mjs"
  },
  "dependencies": {
    "dockerode": "^4.0.5"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  },
  "engines": { "node": ">=20" }
}
